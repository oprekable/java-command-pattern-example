language: java
sudo: false

jdk:
  - openjdk8
cache:
  directories:
    - "$HOME/.m2/repository"
install: "/bin/true"
stage: Unit Tests
  script:
    - mvn clean package
    - mvn cobertura:cobertura
  after_success:
    - bash <(curl -s https://codecov.io/bash)